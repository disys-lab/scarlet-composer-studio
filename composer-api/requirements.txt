fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pyjwt>=2.8.0
requests>=2.28.0
pyyaml>=6.0

# scarlets (Messenger, redisConnect) - installed from the repo root this
# directory lives alongside (../setup.py). For local dev:
#   pip install -e ..
# In a real deployment, install from Gemfury/PyPI like any other consumer
# of the scarlets package - not vendored or reimplemented here.
#
# scarletcomposer (routers/scarlets.py reuses ScarletInterpreter/
# ScarletHandler's existing #scarlet-comment parsing rather than
# reimplementing it) - a *second* package built from a non-default setup
# file (../setup_composer.py), so `pip install -e ..` alone doesn't install
# it. For local dev, from the repo root:
#   python setup_composer.py develop
